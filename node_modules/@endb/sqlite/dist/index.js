"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const sql_1 = __importDefault(require("@endb/sql"));
const sqlite3_1 = require("sqlite3");
const util_1 = require("util");
class EndbSqlite extends sql_1.default {
    constructor(options = {}) {
        const { uri = 'sqlite://:memory:' } = options;
        super({
            dialect: 'sqlite',
            async connect() {
                return new Promise((resolve, reject) => {
                    const path = uri.replace(/^sqlite:\/\//, '');
                    const db = new sqlite3_1.Database(path, (error) => {
                        if (error) {
                            reject(error);
                        }
                        else {
                            if (options.busyTimeout) {
                                db.configure('busyTimeout', options.busyTimeout);
                            }
                            resolve(util_1.promisify(db.all.bind(db)));
                        }
                    });
                });
            },
            ...options,
        });
    }
}
exports.default = EndbSqlite;

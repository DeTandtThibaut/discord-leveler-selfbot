import { Query } from '../node';
import { Table } from '../table';
export declare abstract class Dialect {
    config: any;
    constructor(config: any);
    abstract getQuery(queryNode: Query<any> | Table<any>): {
        text: string;
        values: string[];
    };
    abstract getString(queryNode: Query<any>): string;
}

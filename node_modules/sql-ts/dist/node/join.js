'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
const tableTracker_1 = require("../tableTracker");
class JoinNode extends _1.Node {
    constructor(subType, from, to) {
        super('JOIN');
        this.sql = (tableTracker_1.hasTable(from) && from.table.sql) || (tableTracker_1.hasTable(to) && to.table.sql) || undefined;
        this.subType = subType;
        this.from = from.toNode();
        this.to = to.toNode();
    }
    on(node) {
        this.onNode = node;
        return this;
    }
    join(other) {
        return new JoinNode('INNER', this, other);
    }
    leftJoin(other) {
        return new JoinNode('LEFT', this, other);
    }
}
exports.JoinNode = JoinNode;
//# sourceMappingURL=join.js.map
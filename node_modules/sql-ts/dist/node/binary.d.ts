import { IAliasMixin, IValueExpressionMixin, Node } from '.';
export declare class BinaryNode extends Node {
    left: Node;
    operator: string;
    right: Node;
    constructor(config: {
        left: Node;
        operator: string;
        right: Node;
    });
}
export interface BinaryNode extends IValueExpressionMixin, IAliasMixin {
}

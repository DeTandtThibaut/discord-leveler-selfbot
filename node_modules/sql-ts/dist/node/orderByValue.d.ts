import { Node, TextNode } from '.';
export declare class OrderByValueNode extends Node {
    value: Node;
    direction?: TextNode;
    constructor(config: {
        value: Node;
        direction?: TextNode;
    });
}

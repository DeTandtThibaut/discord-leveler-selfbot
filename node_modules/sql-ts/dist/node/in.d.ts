import { IAliasMixin, IValueExpressionMixin, Node } from '.';
export declare class InNode extends Node {
    left: Node;
    right: Node;
    constructor(config: {
        left: Node;
        right: Node;
    });
}
export interface InNode extends IValueExpressionMixin, IAliasMixin {
}

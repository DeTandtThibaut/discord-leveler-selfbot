'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const _ = require("lodash");
const _1 = require(".");
let valueExpressionMixed = false;
class NotInNode extends _1.Node {
    constructor(config) {
        super('NOT IN');
        this.left = config.left;
        this.right = config.right;
        // Delay mixin to runtime, when all nodes have been defined, and
        // mixin only once. ValueExpressionMixin has circular dependencies.
        if (!valueExpressionMixed) {
            valueExpressionMixed = true;
            _.extend(NotInNode.prototype, _1.valueExpressionMixin());
        }
    }
}
exports.NotInNode = NotInNode;
_.extend(NotInNode.prototype, _1.AliasNode.AliasMixin);
//# sourceMappingURL=notIn.js.map
import { INodeable } from '../nodeable';
import { Sql } from '../sql';
export declare abstract class Node implements INodeable {
    sql?: Sql;
    readonly type: string;
    nodes: Node[];
    constructor(type: string);
    toNode(): this;
    add(node: Node | INodeable | string): this;
    unshift(node: Node | INodeable | string): this;
    toQuery(dialect?: string): {
        text: string;
        values: string[];
    };
    toNamedQuery(name: string, dialect?: string): {
        name: string;
        text: string;
        values: string[];
    };
    toString(dialect?: string): string;
    addAll(nodes: Array<Node | INodeable | string>): this;
}

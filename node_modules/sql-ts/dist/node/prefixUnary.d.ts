import { IAliasMixin, IValueExpressionMixin, Node } from '.';
export declare class PrefixUnaryNode extends Node {
    left: Node;
    operator: string;
    constructor(config: {
        left: Node;
        operator: string;
    });
}
export interface PrefixUnaryNode extends IValueExpressionMixin, IAliasMixin {
}

import { Node } from '.';
export declare class OnConflictNode extends Node {
    columns?: string[];
    constraint?: string;
    update?: string[];
    constructor(config: {
        columns?: string[];
        constraint?: string;
        update?: string[];
    });
}

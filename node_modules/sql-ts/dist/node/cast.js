'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const _ = require("lodash");
const _1 = require(".");
let valueExpressionMixed = false;
class CastNode extends _1.Node {
    constructor(value, dataType) {
        super('CAST');
        this.value = value;
        this.dataType = dataType;
        // Delay mixin to runtime, when all nodes have been defined, and
        // mixin only once. ValueExpressionMixin has circular dependencies.
        if (!valueExpressionMixed) {
            valueExpressionMixed = true;
            _.extend(CastNode.prototype, _1.valueExpressionMixin());
        }
    }
}
exports.CastNode = CastNode;
_.extend(CastNode.prototype, _1.AliasNode.AliasMixin);
//# sourceMappingURL=cast.js.map
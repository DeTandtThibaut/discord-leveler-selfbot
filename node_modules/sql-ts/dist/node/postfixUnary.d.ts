import { IAliasMixin, IValueExpressionMixin, Node } from '.';
export declare class PostfixUnaryNode extends Node {
    left: Node;
    operator: string;
    constructor(config: {
        left: Node;
        operator: string;
    });
}
export interface PostfixUnaryNode extends IValueExpressionMixin, IAliasMixin {
}

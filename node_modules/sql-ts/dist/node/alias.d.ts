import { Node } from '.';
export declare class AliasNode extends Node {
    static AliasMixin: {
        as(this: Node, alias: string): AliasNode;
    };
    value: Node;
    alias: string;
    constructor(value: Node, alias: string);
}
export interface IAliasMixin {
    as(alias: string): AliasNode;
}

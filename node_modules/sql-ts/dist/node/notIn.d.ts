import { IAliasMixin, IValueExpressionMixin, Node } from '.';
export declare class NotInNode extends Node {
    left: Node;
    right: Node;
    constructor(config: {
        left: Node;
        right: Node;
    });
}
export interface NotInNode extends IValueExpressionMixin, IAliasMixin {
}
